# Local development stands up MinIO automatically; no secrets are required.
# The agent also generates the gateway's SOS_AUTH_TOKEN per run and hands it to
# consumers as the secret "token" value of the object-storage configuration
# group — nothing to set here.
# Deployed environments provide SOS_ACCESS_KEY / SOS_SECRET_KEY via a Secret.
